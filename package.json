{
  "name": "@server-state/types",
  "version": "0.2.0",
  "description": "Typescript Type Declarations that are common for Server State projects",
  "types": "types/index.d.ts",
  "scripts": {
    "test": "tsc -p sample",
    "build": "tsc -p .",
    "build:docs": "typedoc && touch docs/.nojekyll",
    "prepublish": "npm run-script build",
    "version": "npm run-script build:docs && git add docs"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/server-state/types.git"
  },
  "author": "Server State",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/server-state/types/issues"
  },
  "homepage": "https://github.com/server-state/types#readme",
  "devDependencies": {
    "typedoc": "^0.20.1",
    "typescript": "^4.0.2"
  },
  "dependencies": {
    "@types/express": "^4.17.2"
  }
}
